import { createTheme } from "@mui/material";

const darkTheme = createTheme({
	palette: {
		mode: "dark",
		primary: {
			main: "#16db65",
		},
		secondary: {
			main: "#058c42",
		},
	},
});

export default darkTheme;
